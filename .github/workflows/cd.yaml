name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. Descargar el código del repositorio
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. Loguearse en Docker Hub
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_TOKEN }}

    # 3. Construir y publicar la imagen Docker desde el código actual
    - name: Build and push Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/myappv2:latest .
        docker push ${{ secrets.DOCKER_USERNAME }}/myappv2:latest

    # 4. Conectarse al servidor EC2 y ejecutar el despliegue
    - name: SSH into EC2 and deploy
      uses: appleboy/ssh-action@v1.2.0
      with:
        host: ${{ secrets.EC2_SERVER }}
        username: ubuntu
        key: ${{ secrets.SERVER_KEY }}
        script: |
          # Actualizar dependencias del servidor
          sudo apt update && sudo apt install -y docker.io docker-compose
          sudo systemctl enable --now docker

          # Crear directorio del proyecto si no existe
          mkdir -p ~/myappv2

          # Subir los ficheros relevantes desde el repositorio
          echo "Uploading docker-compose.yml"
          echo -e "
          version: '3.9'
          services:
            nginx:
              image: ${{ secrets.DOCKER_USERNAME }}/myappv2:latest
              container_name: nginx_server
              ports:
                - '80:80'
              restart: always
          " > ~/myappv2/docker-compose.yml

          # Detener cualquier contenedor en ejecución del proyecto actual
          cd ~/myappv2
          docker-compose down || true

          # Levantar el nuevo contenedor con docker-compose
          docker-compose up -d

          # Verificar contenedores en ejecución
          docker ps
